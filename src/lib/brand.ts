// Shared brand constants.
//
// A trainer's brand colour is a SINGLE solid colour (stored in
// TrainerProfile.emailAccentColor) that drives both the client-app accent and
// the accent strip on outbound emails. When a trainer hasn't picked one, we
// fall back to the PupManager teal below.
export const DEFAULT_BRAND_COLOR = '#2a9da9'
